{{> layout/header}}

<style>
    body {
        font-family: Arial, sans-serif;
    }

    .section-title {
        font-weight: bold;
        margin-top: 2rem;
        color: #343a40;
    }

    .page-title {
        text-align: center;
        font-size: 2rem;
        font-weight: bold;
        color: #212529;
        margin-bottom: 2rem;
        font-family: 'Noto Sans KR', sans-serif;
        letter-spacing: -0.5px;
    }

    .rubric-table {
        width: 100%;
        border-collapse: collapse;
        margin-top: 1rem;
        border-radius: 12px;
        overflow: hidden;
        box-shadow: 0 2px 12px rgba(0, 0, 0, 0.05);
    }

    .rubric-table th,
    .rubric-table td {
        border: 1px solid #dee2e6;
        padding: 16px 20px;
        font-size: 15px;
        vertical-align: top;
    }

    .rubric-table th {
        background-color: #f1f3f5;
        text-align: center;
        font-weight: 600;
        color: #495057;
    }

    .rubric-table td:first-child {
        width: 240px;
        background-color: #f9fafb;
        font-weight: 600;
        color: #343a40;
    }

    .inner-rubric-table {
        width: 100%;
        border-collapse: collapse;
        font-size: 14px;
        background-color: #ffffff;
        border-radius: 8px;
        overflow: hidden;
    }

    .inner-rubric-table th,
    .inner-rubric-table td {
        border: 1px solid #e9ecef;
        padding: 10px 14px;
        line-height: 1.5;
    }

    .inner-rubric-table th {
        background-color: #f8f9fa;
        font-weight: 600;
        text-align: center;
        color: #495057;
    }

    .inner-rubric-table td:first-child {
        width: 80px;
        text-align: center;
        font-weight: bold;
        background-color: #f1f3f5;
    }

    .custom-table {
        width: 100%;
        border-collapse: collapse;
        border: 1px solid #ced4da;
        border-radius: 12px;
        overflow: hidden;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.04);
    }

    .custom-table th {
        background-color: #e9ecef;
        font-weight: bold;
        text-align: center;
        padding: 14px 18px;
        border: 1px solid #ced4da;
        color: #343a40;
    }

    .custom-table td {
        padding: 16px 20px;
        border: 1px solid #dee2e6;
        background-color: #ffffff;
        font-size: 15px;
    }

    .custom-table td.text-start {
        text-align: left;
        color: #495057;
    }


</style>

<div class="container p-5">
    <div class="page-title">
        <h1>평가기준</h1>
    </div>

    <div class="mb-4">
        <h5 class="section-title">※ 훈련목표</h5>
        <p>{{model.subjectPurpose}}</p>
    </div>

    <div class="mb-4">
        <h5 class="section-title">※ 교과목 및 세부내용</h5>
        <table class="custom-table">
            <thead>
            <tr>
                <th>교과목</th>
                <th>세부내용</th>
            </tr>
            </thead>
            <tbody>
            <tr>
                <td>{{model.subjectTitle}}</td>
                <td class="text-start">
                    {{#model.subjectElements}}
                        - {{.}}<br>
                    {{/model.subjectElements}}
                </td>
            </tr>
            </tbody>
        </table>
    </div>

    <div>
        <h5 class="section-title">※ 루브릭(rubric) : 평가기준 요약</h5>

        <table class="rubric-table">
            <thead>
            <tr>
                <th>평가기준</th>
                <th>루브릭</th>
            </tr>
            </thead>
            <tbody>
            {{#model.questions}}
                <tr>
                    <td>{{subjectElementPurpose}}</td>
                    <td>
                        <table class="inner-rubric-table">
                            <thead>
                            <tr>
                                <th>점수</th>
                                <th>채점기준</th>
                            </tr>
                            </thead>
                            <tbody>
                            {{#rubricItems}}
                                <tr>
                                    <td>{{point}}점</td>
                                    <td>{{rubricItem}}</td>
                                </tr>
                            {{/rubricItems}}
                            </tbody>
                        </table>
                    </td>
                </tr>
            {{/model.questions}}
            </tbody>
        </table>
    </div>
</div>

{{> layout/footer}}
