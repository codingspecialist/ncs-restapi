{{> layout/header}}

<div class="container mt-5 mb-5">
    <div class="paper-list-card">
        <div class="list-header">
            <h2>교과목 목록</h2>
        </div>

        <div class="mb-4">
            이 자리에 과정에 대한 전체 결과표 볼 수 있어야 한다
        </div>

        <div class="table-responsive">
            <table class="table table-hover">
                <thead>
                <tr>
                    <th>번호</th>
                    <th>교과목명</th>
                </tr>
                </thead>
                <tbody>
                {{#models}}
                    <tr class="click-tr" onclick="location.href='/api/document-menu/subject/{{subjectId}}'">
                        <td>{{subjectNo}}</td>
                        <td>{{subjectTitle}}</td>
                    </tr>
                {{/models}}
                </tbody>
            </table>
        </div>

    </div>
</div>

{{> layout/footer}}